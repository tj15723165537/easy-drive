spring:
  datasource:
    url: jdbc:mysql://localhost:3306/easy_drive?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=GMT%2B8&allowPublicKeyRetrieval=true
    username: root
    password: Tj123456ww.
    driver-class-name: com.mysql.cj.jdbc.Driver

logging:
  level:
    com.easy.drive.serve: debug
    org.springframework.security: debug

swagger:
  enabled: true
